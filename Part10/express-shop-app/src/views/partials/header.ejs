<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <script type="text/javascript" src="https://code.jquery.com/jquery-1.12.4.min.js"></script>
    <script type="text/javascript" src="https://cdn.iamport.kr/js/iamport.payment-1.1.8.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>Shop App</title>
</head>
<body>

    <nav class="navbar navbar-dark bg-dark shadow-sm fixed-top navbar-expand-lg">
        <div class="container">
            <a href="/" class="narbar-brand">Shop</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarToggle">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="#navbarToggle">
                <% if (currentUser && currentUser.admin == 1) { %>
                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a href="/admin/products" class="nav-link">Products</a>
                        </li>
                        <li class="nav-item">
                            <a href="/admin/categories" class="nav-link">Categories</a>
                        </li>
                    </ul>
                <% } %>

                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    <% if (currentUser) { %>
                        <li class="nav-item">
                            <a href="/cart/checkout" class="nav-link d-flex align-items-center">
                                Cart
                                <span class="ms-1 badge rounded-pill bg-danger">
                                    <% if (cart) { %>
                                        <%= cart.length %>
                                    <% } else { %>
                                        0
                                    <% } %>
                                </span>
                            </a>
                        </li>
                        <li class="nav-item">
                            <form action="/auth/logout" method="POST">
                                <button class="nav-link btn btn-link" type="submit" style="margin-top: -1px;">
                                    Logout
                                </button>
                            </form>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a href="/signup" class="nav-link">Signup</a>
                        </li>
                        <li class="nav-item">
                            <a href="/login" class="nav-link">Login</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
    <br>
    <br>
    <br>

    <div class="container">
        <% if (error && error.length > 0) { %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>
        <% if (success && success.length > 0) { %>
            <div class="alert alert-success" role="alert">
                <%= success %>
            </div>
        <% } %>
    